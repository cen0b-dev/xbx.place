{
  "name": "xbx.place",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:all": "node scripts/dev-all.mjs",
    "supabase:watch": "node scripts/supabase-watch.mjs",
    "supabase:push": "npx supabase db push",
    "supabase:deploy": "bash scripts/supabase-push.sh",
    "build:ia-map": "node scripts/build-ia-map.mjs",
    "build:x360db-catalog": "node scripts/build-x360db-catalog.mjs",
    "build:data": "npm run build:x360db-catalog",
    "build": "npm run build:data && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "@supabase/supabase-js": "^2.106.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
